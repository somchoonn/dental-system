<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ลงทะเบียนทันตแพทย์ | Dentalcare Clinic</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/public/css/main.css">
    <style>
        body {
            background-color: var(--bg, #f6f9fc);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 2rem;
        }
        .register-card {
            background-color: var(--card, #fff);
            border-radius: 14px;
            box-shadow: 0 10px 28px rgba(0, 0, 0, 0.08);
            width: 100%;
            max-width: 800px;
            overflow: hidden;
        }
        .card-header-custom {
            background-color: var(--brand, #2f6fb7);
            color: white;
            padding: 1.5rem 2.5rem;
            text-align: center;
        }
        .card-header-custom h2 {
            margin: 0;
            font-weight: 600;
            font-size: 1.6rem;
        }
        .card-body-custom {
            padding: 2rem 3.5rem 2.5rem;
        }
        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1.25rem 1.5rem;
        }
        .form-group {
            display: flex;
            flex-direction: column;
        }
        .form-group.full-width {
            grid-column: 1 / -1;
        }
        .form-label {
            font-weight: 500;
            color: var(--ink, #333);
            margin-bottom: 0.5rem;
        }
        .form-control, .form-select {
            height: 44px;
            border-radius: 10px;
            border: 1px solid var(--line, #e6eef6);
            padding: 0.75rem 1rem;
            transition: border-color .15s ease-in-out, box-shadow .15s ease-in-out;
        }
        .form-control:focus, .form-select:focus {
            border-color: var(--brand, #2f6fb7);
            outline: 0;
            box-shadow: 0 0 0 0.2rem rgba(47, 111, 183, .25);
        }
        .actions {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 2.5rem;
        }
        .login-link {
            color: var(--muted, #6b7a8a);
        }
        .login-link a {
            color: var(--brand, #2f6fb7);
            font-weight: 600;
            text-decoration: none;
        }
        .btn-primary {
             background-color: var(--brand, #2f6fb7);
             border-color: var(--brand, #2f6fb7);
             color: white;
             padding: 0.7rem 2rem;
             font-weight: 600;
             border-radius: 10px;
        }
         .form-check-label a {
            color: var(--brand, #2f6fb7);
            text-decoration: none;
        }
        .form-check-label a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="register-card">
        <div class="card-header-custom">
            <h2>ลงทะเบียนสำหรับทันตแพทย์</h2>
        </div>
        <div class="card-body-custom">
            <% if (typeof message !== 'undefined' && message) { %>
                <div class="alert alert-danger mb-4"><%= message %></div>
            <% } %>
            <form action="/dentist/register" method="POST">
                <div class="form-grid">
                    <div class="form-group full-width">
                        <label for="license_number" class="form-label">เลขที่ใบประกอบวิชาชีพ</label>
                        <input type="text" class="form-control" id="license_number" name="license_number" required>
                    </div>

                    <div class="form-group">
                        <label for="pre_name" class="form-label">คำนำหน้า</label>
                        <select id="pre_name" name="pre_name" class="form-select">
                            <option value="ทพ." selected>ทพ.</option>
                            <option value="ทพญ.">ทพญ.</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="specialty" class="form-label">ความเชี่ยวชาญ (ถ้ามี)</label>
                        <input type="text" class="form-control" id="specialty" name="specialty">
                    </div>

                    <div class="form-group">
                        <label for="first_name" class="form-label">ชื่อ</label>
                        <input type="text" class="form-control" id="first_name" name="first_name" required>
                    </div>
                    <div class="form-group">
                        <label for="last_name" class="form-label">นามสกุล</label>
                        <input type="text" class="form-control" id="last_name" name="last_name" required>
                    </div>

                    <div class="form-group full-width">
                        <label for="citizen_id" class="form-label">เลขบัตรประชาชน</label>
                        <input type="text" class="form-control" id="citizen_id" name="citizen_id" required maxlength="13">
                    </div>

                    <div class="form-group">
                        <label for="email" class="form-label">อีเมล</label>
                        <input type="email" class="form-control" id="email" name="email" required>
                    </div>
                    <div class="form-group">
                        <label for="phone" class="form-label">เบอร์โทรศัพท์</label>
                        <input type="tel" class="form-control" id="phone" name="phone" required>
                    </div>

                    <div class="form-group">
                        <label for="password" class="form-label">รหัสผ่าน</label>
                        <input type="password" class="form-control" id="password" name="password" required>
                    </div>
                    <div class="form-group">
                        <label for="confirm_password" class="form-label">ยืนยันรหัสผ่าน</label>
                        <input type="password" class="form-control" id="confirm_password" name="confirm_password" required>
                    </div>
                </div>

                <div class="mt-4 mb-3">
                     <label for="terms" style="display: flex; align-items: center; gap: 8px; font-weight: normal; margin-bottom: 0;">
                        <input type="checkbox" id="terms" name="terms" required style="width: auto; height: auto; margin: 0;">
                        <span>ข้าพเจ้ายอมรับ <a href="/terms" target="_blank">เงื่อนไขการให้บริการ</a></span>
                    </label>
                </div>

                <div class="actions">
                    <div class="login-link">
                        <span>มีบัญชีอยู่แล้ว? </span><a href="/login">เข้าสู่ระบบ</a>
                    </div>
                    <button type="submit" class="btn btn-primary" id="submit-btn" disabled>สร้างบัญชี</button>
                </div>
            </form>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script nonce="<%= locals.nonce %>">
        document.addEventListener('DOMContentLoaded', function() {
            const termsCheckbox = document.getElementById('terms');
            const submitButton = document.getElementById('submit-btn');

            termsCheckbox.addEventListener('change', function() {
                submitButton.disabled = !this.checked;
            });
        });
    </script>
</body>
</html>
